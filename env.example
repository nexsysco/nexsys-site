# Environment variables for Nexsys Site
# Copy this file to .env for local development

# Node environment
NODE_ENV=production

# Optional: Custom port (for local development with docker-compose)
PORT=8080

# Optional: Custom base URL (for Dokploy deployment)
BASE_URL=https://your-domain.com